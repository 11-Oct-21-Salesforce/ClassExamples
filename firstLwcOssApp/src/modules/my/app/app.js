/* 
    Our JS modules begin by importing LightningElement from the core lwc module. LightningElement is a wrapper class for a custom
    HTML Element.
*/
import { LightningElement } from 'lwc';

/* 
    We then export the class that we create in our module. This class is the capitalized name of our component by convention
    and it extends the LightningElement class.
*/
export default class App extends LightningElement {

}
