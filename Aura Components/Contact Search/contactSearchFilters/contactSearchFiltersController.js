({
	handleSelectChange : function(component, event, helper) {
        helper.handleSelectChange(component);	
	},
    
    handleSearchInput : function(component, event, helper) {
        helper.handleSearchInput(component);
    }
})